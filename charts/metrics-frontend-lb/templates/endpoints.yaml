apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.service.name }}
subsets:
  - addresses:
      {{- range .Values.backendEndpoints }}
      - ip: {{ .ip }}
      {{- end }}
    ports:
      - port: {{ (index .Values.backendEndpoints 0).port | default 5173 }}

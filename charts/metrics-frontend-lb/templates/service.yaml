apiVersion: v1
kind: Service
  type: ClusterIP
metadata:
  name: {{ .Values.service.name }}
  labels:
    app: metrics-frontend-lb
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
name: Build and Push Docker Images

on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'  # Semantic version tags

jobs:
  build:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && contains(github.event.head_commit.message, 'deploy')
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Generate timestamp
        id: timestamp
        run: echo "timestamp=$(date +%Y%m%d-%H%M%S)" >> $GITHUB_OUTPUT

      - name: Build and push images
        env:
          TIMESTAMP: ${{ steps.timestamp.outputs.timestamp }}
        run: |
          # Backend
          docker build -t tanelsokolov/metrics-backend:latest -t tanelsokolov/metrics-backend:${TIMESTAMP} ./backend
          docker push tanelsokolov/metrics-backend:latest
          docker push tanelsokolov/metrics-backend:${TIMESTAMP}
          
          # Frontend
          docker build -t tanelsokolov/metrics-frontend:latest -t tanelsokolov/metrics-frontend:${TIMESTAMP} ./frontend
          docker push tanelsokolov/metrics-frontend:latest
          docker push tanelsokolov/metrics-frontend:${TIMESTAMP}

  build-semantic:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract version
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Build and push semantic versioned images
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          # Backend
          docker build -t tanelsokolov/metrics-backend:${VERSION} ./backend
          docker push tanelsokolov/metrics-backend:${VERSION}
          
          # Frontend
          docker build -t tanelsokolov/metrics-frontend:${VERSION} ./frontend
          docker push tanelsokolov/metrics-frontend:${VERSION}
